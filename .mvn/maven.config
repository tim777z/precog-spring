# Applied to every Maven invocation in this repository so that local builds, CI and
# container builds behave identically. -ntp (no transfer progress) keeps CI logs readable
# and drops the progress redraw spam that hides real warnings.
-ntp
--batch-mode
